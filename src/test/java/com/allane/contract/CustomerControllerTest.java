package com.allane.contract;

//import static org.junit.Assert.assertEquals;
//import static org.mockito.Mockito.times;
//import static org.mockito.Mockito.verify;
//import static org.mockito.Mockito.when;
//
//import java.sql.Date;
//
//import org.junit.Test;
//import org.junit.runner.RunWith;
//import org.springframework.beans.factory.annotation.Autowired;
//import org.springframework.boot.test.context.SpringBootTest;
//import org.springframework.boot.test.mock.mockito.MockBean;
//import org.springframework.test.context.junit4.SpringRunner;
//
//import com.allane.contract.entity.Customer;
//import com.allane.contract.repository.CustomerRepository;
//import com.allane.contract.service.CustomerService;
//
//
//@RunWith(SpringRunner.class)
//@SpringBootTest
public class CustomerControllerTest {
	
	/*

	@Autowired
	public CustomerService customerService;
	
	@MockBean
	public CustomerRepository customerRepository;
	
	@Test
	public void getCustomerById() throws Exception {
		Long id = 1L;
		Customer newCustomer = new Customer("Micheal", "Faraday", new Date(2022-01-01));
		when(customerRepository.findById(id).get()).thenReturn(newCustomer);
		assertEquals(newCustomer, customerService.getCustomerById(id));
	}
	
	@Test
	public void saveCustomerData() throws Exception {
		Customer newCustomer = new Customer("Micheal", "Faraday", new Date(2022-01-01));
		when(customerRepository.save(newCustomer)).thenReturn(newCustomer);
		assertEquals(newCustomer, customerService.saveCustomerData(newCustomer));
	}
	
	@Test
	public void deleteCustomerData() throws Exception {
		Long id = 1L;
		customerService.deleteById(id);
		verify(customerRepository, times(1)).deleteById(id);
	}
	*/
}
