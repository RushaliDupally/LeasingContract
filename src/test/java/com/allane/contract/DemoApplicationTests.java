package com.allane.contract;

//import static org.junit.Assert.assertEquals;
//import static org.mockito.Mockito.times;
//import static org.mockito.Mockito.verify;
//import static org.mockito.Mockito.when;
//
//import java.sql.Date;
//
//import org.junit.jupiter.api.Test;
//import org.junit.runner.RunWith;
//import org.springframework.beans.factory.annotation.Autowired;
//import org.springframework.boot.test.context.SpringBootTest;
//import org.springframework.boot.test.mock.mockito.MockBean;
//import org.springframework.test.context.junit4.SpringRunner;
//
//import com.allane.contract.entity.Contract;
//import com.allane.contract.entity.Customer;
//import com.allane.contract.entity.Vehicle;
//import com.allane.contract.repository.ContractRepository;
//import com.allane.contract.service.ContractService;
//
//@RunWith(SpringRunner.class)
//@SpringBootTest
class DemoApplicationTests {
	
	
	/*
	@Autowired
	public ContractService contractService;
	
	@MockBean
	public ContractRepository contractRepository;

	@Test
	public void getContractById() {
		Long contractNumber = 11111L;
		Customer cust = new Customer(1L, "Charles", "Elvis", new Date(2022-01-01));
		Vehicle veh = new Vehicle(2L, "BMW", "abc", 2022, 128889d);
		Contract newContract = new Contract(11111L, 12.2, veh, cust);
		when(contractService.getContractById(contractNumber)).thenReturn(newContract);
		assertEquals(contractRepository.findById(contractNumber), newContract);
	}
	
	@Test
	public void saveData() {
		Long contractNumber = 11111L;
		Customer cust = new Customer(1L, "Charles", "Elvis", new Date(2022-01-01));
		Vehicle veh = new Vehicle(2L, "BMW", "abc", 2022, 128889d);
		Contract newContract = new Contract(11111L, 12.2, veh, cust);
		when(contractService.getContractById(contractNumber)).thenReturn(newContract);
		assertEquals(contractRepository.findById(contractNumber), newContract);
	}
	
	@Test
	public void deleteContractById() {
		Long contractNumber = 11111L;
		contractService.deleteById(contractNumber);
		verify(contractRepository, times(1)).deleteById(contractNumber);
	}
	*/

}
